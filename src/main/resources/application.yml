spring:
  datasource:
    url: jdbc:mysql://db4free.net:3306/mkrewsda
    username: sanoczek
    password: Piekna13#
  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: mkrew.kontakt@gmail.com
    password: mkrew.sda
    default-encoding: UTF-8
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true


info:
  app:
    profile: ${spring.profiles.active}
  serverInfo:
    os: ${os.name}
  user:
    timeZone: ${user.timezone}
    country: ${user.country}
  metrics:

management:
  endpoints:
    web:
      exposure:
        include: '*'

springfox:
  documentation:
    swagger-ui:
      enabled: true