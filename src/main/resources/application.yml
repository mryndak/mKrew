spring:
  datasource:
    url: jdbc:mysql://sql11.freemysqlhosting.net:3306/sql11454700
    username: sql11454700
    password: PbzcSaA4Vm
  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: mkrew.kontakt@gmail.com
    password: mkrew.sda
    default-encoding: UTF-8
    protocol: smtp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

info:
  app:
    profile: ${spring.profiles.active}
  serverInfo:
    os: ${os.name}
  user:
    timeZone: ${user.timezone}
    country: ${user.country}
  metrics:

management:
  endpoints:
    web:
      exposure:
        include: '*'

springfox:
  documentation:
    swagger-ui:
      enabled: true